# Discord Roleplay Bot - วิธี Deploy บน Hosting ฟรี

## ✅ ความพร้อมของโปรเจค
Bot นี้พร้อม deploy แล้ว! มี dependencies ที่จำเป็นครบถ้วน:
- ✅ `discord.js` - Discord Bot Framework
- ✅ `groq-sdk` - AI Chat API
- ✅ `dotenv` - Environment Variables
- ✅ `package.json` - สำหรับติดตั้ง dependencies

---

## 🌟 แนะนำ Hosting Platform ฟรี (เรียงจากดีที่สุด)

### 1. **Render.com** ⭐ (แนะนำสุด)
**ข้อดี:**
- ✅ Free tier: 750 ชั่วโมง/เดือน
- ✅ ไม่ต้องใส่บัตรเครดิต
- ✅ Auto deploy จาก GitHub
- ✅ Bot รันต่อเนื่องได้ (ไม่ sleep)

**วิธี Deploy:**
1. Push โค้ดขึ้น GitHub (ถ้ายังไม่มี repo)
2. สมัคร [render.com](https://render.com)
3. เลือก **New Web Service**
4. เชื่อมต่อ GitHub repo
5. ตั้งค่า:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
6. เพิ่ม Environment Variables:
   - `DISCORD_TOKEN`
   - `GROQ_API_KEY`
7. กด **Create Web Service**

---

### 2. **Railway.app** 🚂
**ข้อดี:**
- ✅ Free tier: $5 credit/เดือน (~500 ชั่วโมง)
- ✅ UI สวย ใช้งานง่ายมาก
- ✅ Auto deploy จาก GitHub

**วิธี Deploy:**
1. สมัคร [railway.app](https://railway.app)
2. เลือก **New Project** → **Deploy from GitHub repo**
3. เลือก repo ของ bot
4. Railway จะ detect `package.json` อัตโนมัติ
5. เพิ่ม Environment Variables ในแท็บ **Variables**:
   - `DISCORD_TOKEN`
   - `GROQ_API_KEY`
6. Deploy อัตโนมัติ!

---

### 3. **Glitch.com** 🎪
**ข้อดี:**
- ✅ ฟรี 100%
- ✅ แก้โค้ดใน browser ได้เลย

**ข้อเสีย:**
- ⚠️ จะ sleep หลัง 5 นาทีไม่มี HTTP request
- ⚠️ ต้องใช้ Uptime service เสริม (เช่น UptimeRobot)

**วิธี Deploy:**
1. ไปที่ [glitch.com](https://glitch.com)
2. เลือก **New Project** → **Import from GitHub**
3. อัปโหลดไฟล์ทั้งหมดของโปรเจค
4. สร้างไฟล์ `.env` แล้วใส่:
   ```
   DISCORD_TOKEN=your_token_here
   GROQ_API_KEY=your_key_here
   ```
5. Glitch จะรันอัตโนมัติ

**เคล็ดลับ:** ใช้ [UptimeRobot](https://uptimerobot.com) ping URL ของ Glitch ทุก 5 นาทีเพื่อไม่ให้ sleep

---

### 4. **Replit** 💻
**ข้อดี:**
- ✅ ฟรี (แต่มีข้อจำกัด)
- ✅ Code editor + hosting ในที่เดียว

**ข้อเสีย:**
- ⚠️ จะ sleep เมื่อไม่ใช้งาน
- ⚠️ ต้องเปิด browser ค้างไว้ หรือใช้ uptime service

**วิธี Deploy:**
1. ไปที่ [replit.com](https://replit.com)
2. **Create Repl** → เลือก **Import from GitHub**
3. Paste GitHub URL ของโปรเจค
4. ไปที่ **Secrets (Environment Variables)** และเพิ่ม:
   - `DISCORD_TOKEN`
   - `GROQ_API_KEY`
5. กด **Run**

---

## 📋 Checklist ก่อน Deploy

- [ ] มี `package.json` ครบถ้วน ✅ (สร้างให้แล้ว)
- [ ] มี `.gitignore` สำหรับไม่ให้ `.env` ขึ้น GitHub
- [ ] เตรียม `DISCORD_TOKEN` และ `GROQ_API_KEY` ไว้
- [ ] Push โค้ดขึ้น GitHub (ถ้าใช้ Render หรือ Railway)

---

## 🔒 สร้าง `.gitignore` (สำคัญมาก!)

ก่อน push ขึ้น GitHub ต้องมีไฟล์ `.gitignore` เพื่อไม่ให้ secrets รั่วไหล:

```
node_modules/
.env
.env.local
*.log
```

---

## 🚀 คำแนะนำสุดท้าย

**สำหรับผู้เริ่มต้น:** แนะนำ **Railway.app** เพราะ:
- UI ใช้งานง่ายที่สุด
- Auto deploy สะดวก
- ไม่ต้องกังวลเรื่อง sleep

**สำหรับใช้งานจริงระยะยาว:** แนะนำ **Render.com** เพราะ:
- ฟรีชั่วโมงเยอะกว่า (750 ชม./เดือน)
- Reliable และเสถียร
- Bot รันต่อเนื่องไม่หยุด

---

ถ้าต้องการความช่วยเหลือเพิ่มเติมในการ setup GitHub หรือ deploy บอกได้เลยครับ! 🎉
